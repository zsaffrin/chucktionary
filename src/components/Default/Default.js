import React from 'react'

const Default = () => {
  const styles = {
    page: {
      display: 'grid',
      alignItems: 'center',
      justifyContent: 'center',
    },
  }

  return (
    <div style={styles.page}>
      LOADIGN...
    </div>
  )
}

export default Default
